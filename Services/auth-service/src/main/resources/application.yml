server:
  port: 8081

spring:
  application:
    name: auth-service
  config:
    import: "configserver:http://localhost:8888"
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
    bootstrap:
      enabled: true