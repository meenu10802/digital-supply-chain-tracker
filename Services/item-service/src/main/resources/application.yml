server:
  port: 8083

spring:
  application:
    name: item-service
  config:
    import: "configserver:http://localhost:8888"
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
    bootstrap:
      enabled: true
